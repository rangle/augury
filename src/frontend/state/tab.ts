export enum Tab {
  /// A tree representation of application components
  ComponentTree,

  /// A tree of router paths
  RouterTree
}

export enum StateTab {
  /// Properties panel
  Properties,

  /// Injector graph
  InjectorGraph
}

